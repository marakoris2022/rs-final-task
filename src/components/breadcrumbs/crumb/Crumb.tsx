/* import { useLocation, useMatch, useHistory } from "react-router-dom";
import styles from '.'
export type CrumbProps = {
  className?: string;
  path: string;
  url?: string;
  title?: string;
};

export const Crumb = ({ className: string, path }) => {
  const history = useHistory();
  const location = useLocation();
  const match = useMatch();

  return <span className={`${styles.crumb} ${className}`}></span>;
}
 */
